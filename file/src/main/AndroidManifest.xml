<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zp.file">

    <application>
        <activity android:name=".ui.FileManageActivity" />
        <activity android:name=".ui.PicActivity" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.fileManager.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_manage_paths" />
        </provider>

        <activity android:name=".ui.VideoPlayActivity" />
    </application>

</manifest>